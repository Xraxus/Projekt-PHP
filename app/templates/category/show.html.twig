{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.taskView'|trans({'%id%': category.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1> {{ 'title.taskView'|trans({'%id%': category.id|default('')}) }} </h1>

    {% if category is defined and category|length %}

        <dl class="dl-horizontal">
            <dt> ID </dt>
            <dd> {{ category.id }} </dd>
            <dt> Created at </dt>
            <dd> {{ category.createdAt|date('Y-m-d') }} </dd>
            <dt> Updated at </dt>
            <dd> {{ category.updatedAt|date('Y-m-d') }} </dd>
            <dt> Title </dt>
            <dd> {{ category.title }} </dd>
        </dl>
        <p>
            <a href="{{ url('task_index') }}" title="Back to list">
                Back to list
            </a>
        </p>

    {% else %}

        <p>
            Record not found!
        </p>

    {% endif %}

{% endblock %}